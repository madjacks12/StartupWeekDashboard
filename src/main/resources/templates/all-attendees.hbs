{{#partial "content"}}
<h1 Attendee List</h1>
{{#if attendees}}
<h2>All Attendees:</h2>
<table class="table table-striped table-dark">
    <thead>
    <tr>
        <th scope="col"></th>
        <th scope="col">First Name:</th>
        <th scope="col">last Name:</th>
        <th scope="col">email:</th>
        <th scope="col">phone:</th>

    </tr>
    </thead>
    {{#each attendees}}
    <tbody>
    <tr>
        <th scope="row"></th>
        <td>{{firstName}}</td>
        <td>{{lastName}}</td>
        <td>{{email}}</td>
        <td>{{phone}}</td>
        <td><a href="/events/{{event.id}}/attendees/{{id}}/update" type="click" class="btn btn-success">Edit</a></td>
    </tr>
    </tbody>
    {{/each}}
</table>
{{else}}
<p>No Attendees added yet</p>
{{/if}}

{{/partial}}

{{> layout.hbs}}